{
  "name": "chrome-extension",
  "version": "1.0.2",
  "private": true,
  "description": "Sample chrome extension that uses FingerprintJS FingerprintJS Pro JavaScript agent.",
  "scripts": {
    "build": "vite build",
    "zip": "yarn run build && cd build && zip -r ../build.zip *",
    "watch": "vite build --watch",
    "release": "semantic-release",
    "release-dry": "semantic-release --dry-run",
    "publish-to-store": "yarn run zip && node scripts/publish.mjs"
  },
  "release": {
    "extends": "semantic-release-monorepo",
    "tagFormat": "chrome-extension-v${version}",
    "branches": [
      "main"
    ],
    "plugins": [
      "@semantic-release/commit-analyzer",
      "@semantic-release/release-notes-generator",
      "@semantic-release/changelog",
      "@semantic-release/npm",
      [
        "@semantic-release/git",
        {
          "assets": [
            "CHANGELOG.md",
            "package.json"
          ]
        }
      ],
      "@semantic-release/github"
    ]
  },
  "devDependencies": {
    "chrome-webstore-upload": "^1.0.0"
  }
}
